/**
 * Version de l'app : source unique.
 * Chargée en script classique par `index.html` et par `importScripts()` dans `sw.js`
 * (nom du cache = "compteur-wc-" + APP_VERSION).
 * Incrémenter ici à chaque mise en ligne.
 */
self.APP_VERSION = '1.0.0';
